/*package com.example.playertest;

import com.example.client.model.Player;

import com.example.client.ClientApplication;

import org.junit.Assert;
import org.junit.FixMethodOrder;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.junit.runners.MethodSorters;
import org.mockito.Mockito;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import java.util.List;

import static org.junit.Assert.assertEquals;
import static org.mockito.Mockito.doThrow;

@ActiveProfiles("testPlayer")
@RunWith(SpringJUnit4ClassRunner.class)
@SpringBootTest(classes = ClientApplication.class)
@FixMethodOrder(MethodSorters.NAME_ASCENDING)
public class PlayerTest {
    
    private Player mock = org.mockito.Mockito.mock(Player.class);

    @Autowired
    private Player player;

    private Player newPlayer = new Player();
    private Player newPlayer2 = new Player();

    @Test
    public void aGetHealthTest() {
    }

}
*/

